name: ripo_local_api
description: Localhost-only REST API for RIPO using Shelf + SQLite.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  crypto: ^3.0.3
  path: ^1.9.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^2.4.6

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8
